export type VisitorGuidelinesContent = {
  topHeading: string;
  heading: string;
  description: string;
  btnText: string;
  image: string;
  guidelinesUrl: string;
};

export type VisitorGuidelinesBlockProps = {
  content?: VisitorGuidelinesContent;
};
